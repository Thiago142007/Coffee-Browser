{
  "name": "coffee-browser",
  "version": "1.0.0",
  "description": "Coffee Browser — Navegador Web Desktop com Privacidade Brave",
  "main": "main.js",
  "author": "Coffee Browser Team",
  "scripts": {
    "start": "electron .",
    "installer": "electron ../Installer",
    "build:exe": "npx electron-packager . CoffeeBrowser --platform=win32 --arch=x64 --out=dist --overwrite",
    "build:linux": "npx electron-packager . CoffeeBrowser --platform=linux --arch=x64 --out=dist --overwrite",
    "build:linux-arm64": "npx electron-packager . CoffeeBrowser --platform=linux --arch=arm64 --out=dist --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^19.0.2",
    "electron": "^34.3.0"
  }
}
